spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:mysql://localhost:3306/techforge?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: kang2005.

jwt:
  secret: TechForge-Local-Dev-Secret-Key-For-Testing-Purposes-Only-256BitsLong

logging:
  level:
    com.techforge: DEBUG
    org.hibernate.SQL: DEBUG